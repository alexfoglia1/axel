git checkout dev_arch && git pull origin master && git push origin dev_arch && git checkout master
git checkout dev_drivers && git pull origin master && git push origin dev_drivers && git checkout master
git checkout dev_exceptions && git pull origin master && git push origin dev_exceptions && git checkout master
git checkout dev_filesystem && git pull origin master && git push origin dev_filesystem && git checkout master
git checkout dev_hmi && git pull origin master && git push origin dev_hmi && git checkout master
git checkout dev_libc && git pull origin master && git push origin dev_libc && git checkout master
git checkout dev_stub && git pull origin master && git push origin dev_stub && git checkout master
  
  
  
  
  
  

